<view class="content" >
 <ArticleItem wx:for="{{itemList}}" article="{{item}}" key="index"> </ArticleItem> 
</view>

<button 
  wx:if='{{showLoginButton}}'
  class='an-button primary but big login button-hover' 
  bindtap='linkToLogin'>前往登录</button>